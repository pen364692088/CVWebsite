<footer>
  <div class="container footer-inner">
    <div class="footer-content">
      <p class="footer-copyright">© {new Date().getFullYear()} Pen N. Crafted with Astro.</p>
      <div class="footer-links">
        <a href="mailto:hello@placeholder.dev" class="footer-link">
          <span class="link-text">hello@placeholder.dev</span>
          <span class="link-indicator"></span>
        </a>
      </div>
    </div>
    <div class="footer-glow"></div>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--line);
    padding: var(--space-xl) 0 var(--space-2xl);
    margin-top: var(--space-2xl);
    position: relative;
    background: color-mix(in srgb, var(--bg-secondary) 80%, transparent);
    backdrop-filter: blur(10px);
  }

  footer::before {
    content: '';
    position: absolute;
    top: -1px;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--accent), var(--accent-secondary), var(--accent));
    opacity: 0.6;
    animation: footer-scanline 4s ease-in-out infinite;
  }

  @keyframes footer-scanline {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 0.8; }
  }

  .footer-inner {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-lg);
    flex-wrap: wrap;
  }

  .footer-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
    text-align: center;
  }

  .footer-copyright {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.9rem;
    background: linear-gradient(135deg, var(--text-secondary), var(--accent));
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 0 10px var(--accent-soft);
  }

  .footer-links {
    display: flex;
    gap: var(--space-md);
  }

  .footer-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    color: var(--accent);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-sm);
    background: color-mix(in srgb, var(--surface) 60%, transparent);
    border: 1px solid var(--line);
    transition: all var(--transition-normal) ease;
    position: relative;
    overflow: hidden;
  }

  .footer-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, var(--accent), transparent);
    transition: left var(--transition-slow) ease;
    opacity: 0.3;
  }

  .footer-link:hover {
    color: var(--bg);
    background: var(--accent);
    border-color: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: 
      0 0 20px var(--accent),
      0 0 30px var(--accent-soft);
  }

  .footer-link:hover::before {
    left: 100%;
  }

  .link-text {
    position: relative;
    z-index: 1;
  }

  .link-indicator {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    opacity: 0.7;
    transition: all var(--transition-normal) ease;
    animation: indicator-pulse 2s ease-in-out infinite;
  }

  @keyframes indicator-pulse {
    0%, 100% { 
      opacity: 0.4; 
      transform: scale(0.8);
    }
    50% { 
      opacity: 1; 
      transform: scale(1.2);
    }
  }

  .footer-link:hover .link-indicator {
    background: var(--bg);
    opacity: 1;
    animation: indicator-pulse-fast 1s ease-in-out infinite;
  }

  @keyframes indicator-pulse-fast {
    0%, 100% { 
      opacity: 0.8; 
      transform: scale(0.9);
    }
    50% { 
      opacity: 1; 
      transform: scale(1.3);
    }
  }

  .footer-glow {
    position: absolute;
    bottom: -20px;
    left: 50%;
    transform: translateX(-50%);
    width: 200px;
    height: 20px;
    background: radial-gradient(ellipse at center, var(--accent-soft) 0%, transparent 70%);
    opacity: 0.4;
    animation: footer-glow-pulse 6s ease-in-out infinite alternate;
  }

  @keyframes footer-glow-pulse {
    0% { 
      opacity: 0.2; 
      width: 150px;
    }
    100% { 
      opacity: 0.6; 
      width: 250px;
    }
  }

  /* Mobile responsiveness */
  @media (max-width: 768px) {
    footer {
      padding: var(--space-lg) 0 var(--space-xl);
    }
    
    .footer-content {
      gap: var(--space-md);
    }
    
    .footer-links {
      flex-direction: column;
      gap: var(--space-sm);
    }
  }

  /* Accessibility: reduce motion for users who prefer it */
  @media (prefers-reduced-motion: reduce) {
    .footer-link::before,
    .footer-link:hover::before,
    .link-indicator,
    footer::before,
    .footer-glow {
      animation: none !important;
      transition: none !important;
    }
    
    .footer-link:hover {
      transform: none;
    }
  }
</style>
